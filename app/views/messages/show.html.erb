<%= @message.title %>
<%= @message.content %>
<%= link_to "Edit this message", edit_message_path %>
<%= link_to "Delete this message", message_path(@message), method: :delete, data: { confirm: "Are you sure you want to delete this message?" } %>
